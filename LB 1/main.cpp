#include <iostream>
#include <curses.h>
#include "BinInOut.h"

int main() {
    initscr();
    int n;
    std::cin >> n;
    // std::cout << "Binary representation: ";
    // BinOut((unsigned char*)&n, sizeof(n));

}